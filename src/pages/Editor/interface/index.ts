export interface FormValues {
  title: string;
  description: string;
  content: string;
}

export interface FormProps {
  title?: string;
  description?: string;
  content?: string;
}

export interface Slug {
  slug: string;
}
